# Changelog

## v0.6.0

### ✨ 新增
- 字体大小下拉框，支持多选；多选字体大小不一致时显示空白“字号”
- 加粗 / 斜体 / 下划线，多选时能识别统一状态
- 字体颜色菜单：20 个标准色 + 自定义色盘
- 背景颜色菜单：20 个标准色 + 自定义色盘
- 选中带底色格子时，多选淡蓝色半透明叠加，不再遮住底色
- 点击空白处取消选中

### 🐛 修复
- 保存时重建整个 xlsx，避免样式表逐步损坏导致“只有第一次保存正常”
- 修复合并单元格读取正则错误，合并保存刷新后不再丢失
- 修复底色保存后“反色”问题（空 fill 导致样式错乱）
- 修复单击无反应 / 多选残留范围误改问题
- 修复点击空白取消选中后报错问题
- 列宽 / 行高拉伸比例调整，更接近 Excel 手感
- 禁用重型快照日志，避免输入时卡死

### 🧹 其他
- 文档已重置为默认格式
- 清理调试日志
- 更新版本号与内部模块 ID，避免缓存干扰
